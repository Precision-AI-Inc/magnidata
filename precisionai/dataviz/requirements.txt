# Dataviz feature extractor — pinned for reproducible feature extraction.
# Exact pins (not ranges): float results can drift across library versions, and the
# NIMA stack (pyiqa) constrains numpy to 2.x, so the whole set must be consistent.
# These are the versions verified together (tests pass, output byte-stable on re-run).

# ── Core (pixel / COCO / metadata features) ──────────────────────────────────────
numpy==2.4.6        # required by pyiqa; tool also passes under 1.26.4 for skip-nima runs
pillow==10.4.0
tqdm==4.68.3        # progress bar (optional — tool falls back to periodic prints)

# ── NIMA image quality (PyTorch via pyiqa) ───────────────────────────────────────
# torch is large. For a CPU-only box install the matching CPU build first:
#   pip install torch==2.12.1 torchvision==0.27.1 --index-url https://download.pytorch.org/whl/cpu
# then `pip install -r requirements.txt`. On a GPU box the default index is fine.
torch==2.12.1
torchvision==0.27.1
pyiqa==0.1.13
timm==1.0.27        # pyiqa backbone provider — pinned for reproducible weights/arch

# ── Dev ──────────────────────────────────────────────────────────────────────────
pytest==9.1.1
